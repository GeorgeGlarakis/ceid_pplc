PROGRAM pplc

FUNCTION func (INTEGER d)

	WHILE (g!=y) 
		d = !h;
		j = -4;
	ENDWHILE


END_FUNCTION


STARTMAIN() 
VARS INTEGER s,j;
kab = 43 / 5 ;
FOR i=0 TO 10 STEP 1
	
	IF(k == 3) THEN
		k = k - 1;
	ELSEIF(kj<6)
		j = h;
		k_few = f + 5;
	ELSEIF(kj>6)
		j = h;
		k_few = f + 5;
	ELSE 
		k = k + 1;
	ENDIF
		
ENDFOR

PRINT("Iteration: Number: ", i,k);
	
ENDMAIN	